version: '2'

services:

  svc-example:
    build: ..
    image: quay.io/guidesmiths/svc-example
    links:
     - mongo:mongo
     - redis:redis
     - vault:vault

  mongo:
    image: mongo

  redis:
    image: redis

  vault:
    image: sjourdan/vault
    hostname: vault
